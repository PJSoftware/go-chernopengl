# Modern OpenGL

For `C++`, The Cherno talks about libraries such as `GLEW` and `GLAD` (he'll be using `GLEW`) and where to get them, how to incorporate them into a Visual Studio project, etc.

For `Go` we just want to do:

```sh
go get github.com/go-gl/gl/v4.1-core/gl
```
